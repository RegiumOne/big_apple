class AppDuration {
  AppDuration._();

  static const double defaultAnimationSpeed = 0.25;
  static const Duration saveGameDuration = Duration(minutes: 1);

  static const double buildingIncomeIntervalInSeconds = 12;
}
